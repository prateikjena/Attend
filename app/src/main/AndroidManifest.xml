<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.attend">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".attendance.Batches"></activity>
        <activity android:name=".attendance.TakeAttendance" />
        <activity android:name=".authenticate.ForgotPassword" />
        <activity android:name=".authenticate.Authenticate" />
        <activity android:name=".authenticate.SignUp" />
        <activity android:name=".authenticate.Login" />
        <activity android:name=".attendance.AttendanceSheet" />
        <activity
            android:name=".slider.Slider"
            android:noHistory="true" />
        <activity
            android:name=".MainActivity"
            android:noHistory="true" />
        <activity
            android:name=".welcome.Welcome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Splash"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>